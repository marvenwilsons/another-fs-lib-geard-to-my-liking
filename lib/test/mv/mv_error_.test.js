const FS = require('../../lib_dqfs.exts')
const nodefs = require('fs')
const fs = new FS

// moving a file in a dir, into a file in a directory
// ERROR: dist file exist

// moving a dir into a file
// ERROR: logic error, error moving a dir into a file